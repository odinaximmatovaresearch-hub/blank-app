import streamlit as st
import pandas as pd

st.title("📁 CSV yuklash")

uploaded_file = st.file_uploader("CSV faylni tanlang", type=["csv"])
if uploaded_file:
    df = pd.read_csv(uploaded_file)
    st.success("✅ Fayl muvaffaqiyatli yuklandi!")
    st.dataframe(df)



